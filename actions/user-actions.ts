"use server"

export async function saltAndHashPassword(password : string) {
    try {
       
    } catch (error) {
        console.log(error);
        return false;
    }
};

export async function getUserFromDb(email : string) {
    try {
        
    } catch (error) {
        console.log(error);
        return false;
    }
};

export async function CheckIfHashMatched(ipPassword : string, hashedPassword : string) {
    try {
        
    } catch (error) {
        console.log(error);
        return false;
    }
}